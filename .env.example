# Gemini API キー（必須）
# Google AI Studio で取得: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# MCP認証トークン（オプション、本番環境では推奨）
# 形式: token1:client_id1,token2:client_id2
# 例: MCP_AUTH_TOKENS=my-secret-token-123:claude-desktop,another-token:mobile-app
# 強力なトークン生成: openssl rand -base64 32
MCP_AUTH_TOKENS=

# MCP トランスポートモード（オプション、デフォルト: stdio）
# 選択肢: stdio（ローカル）、http（Cloud Run等）
MCP_TRANSPORT=stdio

# サーバーポート（オプション、デフォルト: 8080）
PORT=8080

# ログレベル（オプション、デフォルト: INFO）
# 選択肢: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO
